/** RevenueCat public SDK key (iOS). Safe to ship in the client. */
export const REVENUECAT_API_KEY = 'appl_NTVvXkGFdYOClCyOcVdvCUjtIZP';

/** RevenueCat / App Store product identifiers. */
export const PRODUCT_IDS = {
  weekly: 'com.cluent.weekly',
  yearly: 'com.cluent.yearly',
} as const;

/** Entitlement that unlocks Fluent Pro. */
export const PREMIUM_ENTITLEMENT_ID = 'pro';
