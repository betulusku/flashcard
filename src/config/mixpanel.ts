/**
 * Mixpanel project token (Project Settings → Access Keys).
 * Safe to ship in the client SDK — do NOT put the API Secret here;
 * that key is for server/export APIs only.
 */
export const MIXPANEL_TOKEN = '88d07ecf62ef042238a370a4d249f4df';

/** Must match Project Settings → Data Residency (US). */
export const MIXPANEL_SERVER_URL = 'https://api.mixpanel.com';
